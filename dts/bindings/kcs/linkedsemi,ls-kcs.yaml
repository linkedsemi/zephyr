description: Linkedsemi KCS controller

compatible: "linkedsemi,ls-kcs"

include: base.yaml

properties:
    kcs-env-addr:
      type: int
      required: true
    port:
      type: array
      required: true

    mbox:
      type: phandle