
description: Linkedsemi LPC controller

compatible: "linkedsemi,ls-lpc"

include: [base.yaml, pinctrl-device.yaml, reset-device.yaml]

properties:
  reg:
    required: true

  interrupts:
    required: true
