
description: Linkedsemi eSPI slave

compatible: "linkedsemi,ls-espi"

include: [espi-controller.yaml, pinctrl-device.yaml, reset-device.yaml]

properties:
  reg:
    required: true

  interrupts:
    required: true

  cs-gpios:
    type: phandle-array

  sysevent-base:
    type: int
    required: true

  mbox:
    type: phandle