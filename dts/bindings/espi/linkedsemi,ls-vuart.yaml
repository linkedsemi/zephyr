description: Linkedsemi eSPI slave to vuart

compatible: "linkedsemi,ls-vuart"

include: [uart-controller.yaml, pinctrl-device.yaml]

properties:
  port:
    type: int
    required: true

  up-irq:
    type: array
    required: true

  uart:
    type: phandle
