description: LINKEDSEMI UART

compatible: "linkedsemi-uart"

include: [uart-controller.yaml,pinctrl-device.yaml]

properties:
    reg:
      required: true

    current-speed:
      required: true

    interrupts:
      required: false

    # pinctrl-0:
    #   required: true

    # pinctrl-names:
    #   required: true